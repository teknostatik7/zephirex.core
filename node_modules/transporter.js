const nodemailer = require('nodemailer');

var transporter = nodemailer.createTransport({
  service: window.config.senderEmailService,
  auth: {
    user: window.config.senderEmail,
    pass: window.config.senderEmailPassword
  }
});

module.exports = transporter;